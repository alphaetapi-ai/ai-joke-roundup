<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Joke Generator</title>
    <link rel="stylesheet" href="/styles.css" />
</head>
<body>
    <%- include('_header') %>
    <div class="page-layout">
        <div class="recent-list">
            <h2>Recent Jokes</h2>
            <% if (recentJokes.length > 0) { %>
                <table class="compact-jokes-table">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Topic</th>
                            <th>🗳️</th>
                            <th>Preview</th>
                        </tr>
                    </thead>
                    <tbody>
                        <%- include('_joke_table_rows', { jokes: recentJokes, useCompact: true }) %>
                    </tbody>
                </table>
            <% } else { %>
                <p>No jokes yet. Generate your first joke!</p>
            <% } %>
            <p><a href="/recent_jokes">View All Recent Jokes →</a></p>
        </div>
        <div class="generator-form">
            <h1>Joke Generator</h1>
            <p>What would you like your joke to be about?</p>
            <form action="/generate_joke" method="POST">
                <input type="text" name="topic" maxlength="64" placeholder="Enter a topic..." required>
                <br>
                <div class="style-selection">
                    <label>
                        <input type="radio" name="type" value="normal" checked>
                        Normal
                    </label>
                    <label>
                        <input type="radio" name="type" value="story">
                        Story
                    </label>
                    <label>
                        <input type="radio" name="type" value="limerick">
                        Limerick
                    </label>
                </div>
                <button type="submit">Tell me my joke</button>
            </form>
        </div>
        
        <!-- Highest Voted Joke Section - separate from the form -->
        <div class="highest-voted-section">
            <% if (highestVotedJoke) { %>
                <div style="padding: 20px; background-color: #f0f8ff; border-radius: 8px; border: 2px solid #4CAF50;">
                    <h3 style="margin-top: 0; color: #4CAF50;">🏆 Highest Voted Joke:</h3>
                    <p style="font-style: italic; margin-bottom: 10px;">
                        <%= highestVotedJoke.joke_content %>
                    </p>
                    <small style="color: #666;">
                        Topic: <%= highestVotedJoke.topic %> | 
                        Score: +<%= highestVotedJoke.net_score %> 
                        (<%= highestVotedJoke.rating_funny %> funny, <%= highestVotedJoke.rating_okay %> okay, <%= highestVotedJoke.rating_dud %> dud)
                    </small>
                </div>
            <% } else { %>
                <div style="padding: 20px; background-color: #f9f9f9; border-radius: 8px;">
                    <h3 style="margin-top: 0; color: #666;">🏆 Highest Voted Joke:</h3>
                    <p style="color: #666; font-style: italic;">No jokes have been voted on yet. Be the first to vote!</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
